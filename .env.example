# Direct PostgreSQL connection used for an externally managed database.
# `npm run preview:live` creates an isolated local database automatically and does not need this file.
# Never commit real credentials or point this mock-finance app at banking systems.
DATABASE_URL="postgresql://postgres:change-me@127.0.0.1:5432/save_spend?schema=public"

# Optional: freeze API "today" for deterministic goal/recommendation analytics.
# CALCULATION_DATE=2026-07-15T12:00:00.000Z
